// Supabase removed: this stub exists to keep imports from breaking if any remain.
// Do not use in production. It intentionally provides no functionality.
export const supabase = undefined as unknown as {
  from: (..._args: any[]) => never;
};